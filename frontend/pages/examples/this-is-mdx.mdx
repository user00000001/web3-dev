import MdxLayout from "./_components/mdx-layout";

# Somethings

## Some Commands

```shell
pnpm install -D @tailwindcss/typography @next/mdx @mdx-js/loader \
    @mdx-js/react @types/mdx next-mdx-remote raw-loader \
    prism-react-renderer
```

# sdfsdgsd

## dsfsgsdf

```javascript
const a = "a";
```

export default function MDXPage({children}) {
    return <MdxLayout>{children}</MdxLayout>
}